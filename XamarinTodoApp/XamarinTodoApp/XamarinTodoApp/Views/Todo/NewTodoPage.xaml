<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="XamarinTodoApp.Views.Todo.NewTodoPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    xmlns:local="clr-namespace:XamarinTodoApp.ViewModels.Todo"
    Title="New Todo"
    ios:Page.UseSafeArea="true"
    x:DataType="local:NewTodoViewModel"
    Shell.PresentationMode="ModalAnimated">
    <ContentPage.Content>
        <StackLayout Padding="15" Spacing="3">
            <Label FontSize="Medium" Text="Text" />
            <Entry FontSize="Medium" Text="{Binding Text, Mode=TwoWay}" />

            <Label FontSize="Medium" Text="Reward" />
            <Entry
                FontSize="Medium"
                Keyboard="Numeric"
                Text="{Binding Reward, Mode=TwoWay}" />

            <StackLayout Orientation="Horizontal">
                <Button
                    Command="{Binding CancelCommand}"
                    HorizontalOptions="FillAndExpand"
                    Text="Cancel" />
                <Button
                    Command="{Binding SaveCommand}"
                    HorizontalOptions="FillAndExpand"
                    Text="Save" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>
